# Optional. With a Claude API key, the case page drafts its evidence summary and
# can reword a patient letter with Claude. Without one, VariantPulse uses its
# fixed templates and keeps working offline. Put the real key in .env.local,
# which is ignored by git; never commit it.
AI_API_KEY=
